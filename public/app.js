(() => {
  const SITE_URL = "https://energyprojectservices.onrender.com";
  let CONFIG = {};
  let USER = null;
  let currentPage = "Panou principal";
  let industry = "Gaze naturale";
  let workType = "Branșamente gaze naturale";
  const STORAGE_KEY = "eps_state_v2";
  const CHAT_KEY = "eps_ai_dev_chat_v2";
  const pages = ["Panou principal", "Date proiect", "Date tehnice", "Documentație", "Șabloane OSD", "Calcul", "Ștampile", "Email-uri", "Verificări", "Checklist", "Registru proiecte", "Import / Export", "Planuri și licențe", "Marketplace / Module", "Asistent utilizator", "AI Developer", "Inside", "Diagnostic", "Actualizări", "Construire / Lansare"];
  const fields = {"project": ["beneficiar", "adresa_lucrare", "localitate", "judet", "telefon", "email", "osd", "tip_lucrare", "industrie", "numar_contract", "data_contract", "proiectant", "verificator_vgd", "responsabil_rte", "observatii"], "technical": ["debit_instalat", "presiune_regim", "diametru_conducta", "material_conducta", "lungime_bransament", "punct_racordare", "post_reglare", "contor", "categorie_consumator", "traseu", "observatii_tehnice"], "vgd": ["verificator_vgd", "atestat_vgd", "data_verificare_vgd", "status_vgd", "observatii_vgd"], "rte": ["responsabil_rte", "autorizatie_rte", "data_verificare_rte", "status_rte", "observatii_rte"], "email": ["email_destinatar", "email_cc", "subiect_email", "continut_email", "atasamente", "semnatura_email"], "calculation": ["calcul_debit", "calcul_presiune", "pierdere_presiune", "rezultat_calcul", "observatii_calcul"]};
  const documentTemplates = [{"id": "cerere_racordare", "name": "Cerere racordare", "category": "OSD", "body": "Către <osd>,\n\nSubsemnatul/Subscrisa <beneficiar>, solicit racordarea la sistemul de distribuție gaze naturale pentru imobilul situat în <adresa_lucrare>, <localitate>, <judet>.\n\nTip lucrare: <tip_lucrare>.\nTelefon: <telefon>\nEmail: <email>\n\nData: <data_document>\nProiectant: <proiectant>\n<stampila_proiectant>"}, {"id": "memoriu_tehnic", "name": "Memoriu tehnic", "category": "Proiectare", "body": "MEMORIU TEHNIC\n\nBeneficiar: <beneficiar>\nAdresă lucrare: <adresa_lucrare>, <localitate>, <judet>\nOSD: <osd>\nDebit instalat: <debit_instalat>\nPresiune regim: <presiune_regim>\nDiametru conductă: <diametru_conducta>\nMaterial conductă: <material_conducta>\nLungime branșament: <lungime_bransament>\nPunct racordare: <punct_racordare>\nPost reglare: <post_reglare>\nContor: <contor>\nTraseu: <traseu>\n\nObservații tehnice: <observatii_tehnice>\n\nProiectant: <proiectant>\n<stampila_proiectant>"}, {"id": "fisa_date_tehnice", "name": "Fișă date tehnice", "category": "Tehnic", "body": "FIȘĂ DATE TEHNICE\nBeneficiar: <beneficiar>\nCategorie consumator: <categorie_consumator>\nDebit instalat: <debit_instalat>\nPresiune regim: <presiune_regim>\nDiametru conductă: <diametru_conducta>\nMaterial conductă: <material_conducta>\nLungime branșament: <lungime_bransament>"}, {"id": "borderou", "name": "Borderou documente", "category": "Documentație", "body": "BORDEROU DOCUMENTE\n\nBeneficiar: <beneficiar>\nLucrare: <tip_lucrare>\nAdresă: <adresa_lucrare>\n\n1. Cerere racordare\n2. Memoriu tehnic\n3. Fișă date tehnice\n4. Document verificare VGD\n5. Document verificare RTE\n6. Email transmitere documentație"}, {"id": "declaratie_proiectant", "name": "Declarație proiectant", "category": "Proiectare", "body": "DECLARAȚIE PROIECTANT\n\nSubsemnatul <proiectant>, declar că documentația pentru <beneficiar>, <adresa_lucrare>, respectă datele introduse în aplicație.\n\nData: <data_document>\n<stampila_proiectant>"}, {"id": "adresa_osd", "name": "Adresă către OSD", "category": "OSD", "body": "Către <osd>,\n\nVă transmitem documentația aferentă lucrării <tip_lucrare>, beneficiar <beneficiar>, amplasament <adresa_lucrare>, <localitate>, <judet>.\n\nCu stimă,\n<proiectant>"}, {"id": "vgd", "name": "Document verificare VGD", "category": "VGD", "body": "DOCUMENT VERIFICARE VGD\n\nVerificator: <verificator_vgd>\nAtestat VGD: <atestat_vgd>\nData verificare: <data_verificare_vgd>\nStatus: <status_vgd>\nObservații: <observatii_vgd>\n\n<stampila_vgd>"}, {"id": "rte", "name": "Document verificare RTE", "category": "RTE", "body": "DOCUMENT RTE\n\nResponsabil RTE: <responsabil_rte>\nAutorizație RTE: <autorizatie_rte>\nData verificare: <data_verificare_rte>\nStatus: <status_rte>\nObservații: <observatii_rte>\n\n<stampila_rte>"}];
  const emailTemplates = [{"id": "ofertare", "name": "Ofertare", "subject": "Ofertă documentație gaze naturale - <beneficiar>", "body": "Bună ziua,\n\nVă transmitem oferta pentru documentația tehnică aferentă lucrării <tip_lucrare> din <adresa_lucrare>.\n\nCu respect,\nEnergy Project Services"}, {"id": "date_lipsa", "name": "Solicitare date lipsă", "subject": "Date lipsă documentație - <beneficiar>", "body": "Bună ziua,\n\nPentru finalizarea documentației sunt necesare următoarele date: <observatii>.\n\nMulțumim."}, {"id": "transmitere_osd", "name": "Transmitere documentație către OSD", "subject": "Documentație branșament gaze naturale - <beneficiar>", "body": "Către <osd>,\n\nVă transmitem documentația pentru lucrarea situată în <adresa_lucrare>, <localitate>, <judet>.\n\nCu stimă,\n<proiectant>"}, {"id": "vgd", "name": "Transmitere verificare VGD", "subject": "Document verificat VGD - <beneficiar>", "body": "Bună ziua,\n\nDocumentația a fost verificată VGD. Status: <status_vgd>.\nObservații: <observatii_vgd>"}, {"id": "rte", "name": "Transmitere document RTE", "subject": "Document RTE - <beneficiar>", "body": "Bună ziua,\n\nDocumentația RTE are status: <status_rte>.\nObservații: <observatii_rte>"}];
  const defaultState = {
    project: { beneficiar:"", adresa_lucrare:"", localitate:"", judet:"", telefon:"", email:"", osd:"", tip_lucrare:"Branșamente gaze naturale", industrie:"Gaze naturale", numar_contract:"", data_contract:"", proiectant:"", verificator_vgd:"", responsabil_rte:"", observatii:"" },
    technical: { debit_instalat:"", presiune_regim:"", diametru_conducta:"", material_conducta:"", lungime_bransament:"", punct_racordare:"", post_reglare:"", contor:"", categorie_consumator:"", traseu:"", observatii_tehnice:"" },
    vgd: { verificator_vgd:"", atestat_vgd:"", data_verificare_vgd:"", status_vgd:"neverificat", observatii_vgd:"" },
    rte: { responsabil_rte:"", autorizatie_rte:"", data_verificare_rte:"", status_rte:"neverificat", observatii_rte:"" },
    calculation: { calcul_debit:"", calcul_presiune:"", pierdere_presiune:"", rezultat_calcul:"", observatii_calcul:"" },
    stamps: [], documents: [], emails: [], projects: [], updates: [],
    plan: { plan:"Free", status:"activ", activatedAt:new Date().toISOString().slice(0,10), expiresAt:"", exportAllowed:false },
    planRules: { Free:false, Trial:false, Basic:false, Expired:false, Developer:true }
  };
  let state = loadState();

  function deepMerge(a,b){ for(const k in b){ if(b[k] && typeof b[k] === "object" && !Array.isArray(b[k])) a[k]=deepMerge(a[k]||{},b[k]); else a[k]=b[k]; } return a; }
  function loadState(){ try { return deepMerge(structuredClone(defaultState), JSON.parse(localStorage.getItem(STORAGE_KEY)||"{}")); } catch { return structuredClone(defaultState); } }
  function saveState(){ localStorage.setItem(STORAGE_KEY, JSON.stringify(state)); }
  function esc(v){ return String(v ?? "").replace(/[&<>"]/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c])); }
  function today(){ return new Date().toISOString().slice(0,10); }
  function toast(msg){ const t=document.getElementById("toast"); if(!t) return alert(msg); t.textContent=msg; t.classList.remove("hidden"); setTimeout(()=>t.classList.add("hidden"),3000); }
  async function api(url, opts={}){ const r=await fetch(url,{headers:{"Content-Type":"application/json"},...opts}); const txt=await r.text(); try{return JSON.parse(txt);}catch{return {ok:r.ok, raw:txt};} }
  function canExport(){ const plan = state.plan.plan || "Free"; const st=(state.plan.status||"activ").toLowerCase(); if(st.includes("expirat")) return false; return !!state.planRules[plan]; }
  function activeProfile(){ return industry === "Gaze naturale" && workType === "Branșamente gaze naturale"; }
  function planLabel(){ return `${state.plan.plan || "Free"} / export: ${canExport()?"permis":"blocat"}`; }

  async function boot(){ try{ CONFIG = await api("/api/config"); } catch { CONFIG = {}; } fillProfile(); checkLoginHealth(); }
  async function checkLoginHealth(){ try{ const h=await api("/api/health"); const el=document.getElementById("loginHealth"); if(el) el.textContent=`Status: OpenAI=${h.openaiConfigured} GitHub=${h.githubUpdateConfigured} AutoApply=${h.autoApplyGithub}`; } catch {} }

  window.login = async function(){
    const user=document.getElementById("user").value.trim(); const password=document.getElementById("pass").value;
    let res={ok:false}; try{ res=await api("/api/login",{method:"POST",body:JSON.stringify({user,password})}); } catch {}
    if(!res.ok && user.toLowerCase()!=="developer") return alert("Login indisponibil sau date incorecte.");
    USER=res.user || {name:user,role:"Developer",plan:"Developer"};
    if(String(USER.role||"").toLowerCase().includes("developer")){ state.plan.plan="Developer"; state.plan.exportAllowed=true; saveState(); }
    document.getElementById("login").classList.add("hidden"); document.getElementById("app").classList.remove("hidden"); buildNav(); openPage("Panou principal");
  };
  window.showAuthPanel = function(type){ const el=document.getElementById("authPanel"); el.classList.remove("hidden"); el.innerHTML = type==="register" ? `<h3>Cont nou</h3><p class="muted">Interfață pregătită pentru PostgreSQL + AUTH_REGISTER_ENABLED.</p><input placeholder="Email"><input placeholder="Parolă" type="password"><button onclick="toast('Register pregătit pentru backend.')">Creează cont</button>` : `<h3>Recuperare parolă</h3><p class="muted">Trimite email prin SMTP când endpointul este activ.</p><input placeholder="Email"><button onclick="toast('Resetare pregătită pentru SMTP.')">Trimite link resetare</button>`; };
  window.googleLogin = function(){ window.location.href="/api/auth/google"; };

  function fillProfile(){
    const industries=(CONFIG.profiles&&CONFIG.profiles.industries)||["Gaze naturale","Energie electrică","Apă-canal","Telecom / fibră optică","Fotovoltaice","Construcții","Infrastructură feroviară"];
    const workTypes=(CONFIG.profiles&&CONFIG.profiles.workTypes)||["Branșamente gaze naturale","Extindere de conductă gaze naturale","Instalații de utilizare gaze naturale","Modernizări conducte gaze naturale","Studii de fezabilitate gaze naturale"];
    const i=document.getElementById("industry"), w=document.getElementById("workType");
    if(i) i.innerHTML=industries.map(x=>`<option ${x===industry?"selected":""}>${esc(x)}</option>`).join("");
    if(w) w.innerHTML=workTypes.map(x=>`<option ${x===workType?"selected":""}>${esc(x)}</option>`).join("");
    refreshStatus();
  }
  window.profileChanged=function(){ industry=document.getElementById("industry").value; workType=document.getElementById("workType").value; refreshStatus(); openPage(currentPage); };
  function refreshStatus(){ const s=document.getElementById("statusPill"), p=document.getElementById("planPill"); if(s){s.textContent=activeProfile()?"Activ":"Blocat";s.classList.toggle("warn",!activeProfile());} if(p){p.textContent=planLabel();p.classList.toggle("warn",!canExport());} }
  function buildNav(){ document.getElementById("nav").innerHTML=pages.map(p=>`<button class="nav" onclick="openPage('${p.replace(/'/g,"\\'")}')">${p}</button>`).join(""); }
  function setTitle(t,sub=""){ document.getElementById("title").textContent=t; document.getElementById("subtitle").textContent=sub || (activeProfile()?"Profil activ":"Profil blocat până la configurare"); document.querySelectorAll(".nav").forEach(b=>b.classList.toggle("active",b.textContent===t)); }
  function content(html){ document.getElementById("content").innerHTML=html; }
  window.openPage=function(p){ currentPage=p; setTitle(p); refreshStatus(); const map={"Panou principal":pageDashboard,"Date proiect":pageProject,"Date tehnice":pageTechnical,"Documentație":pageDocuments,"Șabloane OSD":pageTemplates,"Calcul":pageCalculation,"Ștampile":pageStamps,"Email-uri":pageEmails,"Verificări":pageVerifications,"Checklist":pageChecklist,"Registru proiecte":pageRegistry,"Import / Export":pageImportExport,"Planuri și licențe":pagePlans,"Marketplace / Module":pageMarketplace,"Asistent utilizator":pageAssistant,"AI Developer":pageAIDeveloper,"Inside":pageInside,"Diagnostic":pageDiagnostic,"Actualizări":pageUpdates,"Construire / Lansare":pageLaunch}; (map[p]||pageGeneric)(p); };

  function labelFor(f){ return f.replaceAll("_"," ").replace(/\b\w/g,c=>c.toUpperCase()); }
  function inputFor(group,f,value){ if(f.includes("observatii")||f==="traseu") return `<textarea onchange="setField('${group}','${f}',this.value)">${esc(value)}</textarea>`; if(f.includes("data")||f==="activatedAt"||f==="expiresAt") return `<input type="date" value="${esc(value)}" onchange="setField('${group}','${f}',this.value)">`; if(f.includes("status")) return `<select onchange="setField('${group}','${f}',this.value)">${["neverificat","în verificare","admis","respins","activ","expirat"].map(x=>`<option ${x===value?"selected":""}>${x}</option>`).join("")}</select>`; return `<input value="${esc(value)}" onchange="setField('${group}','${f}',this.value)">`; }
  function formFor(group, names){ const obj=state[group]||{}; return `<div class="grid">${names.map(f=>`<label>${labelFor(f)}${inputFor(group,f,obj[f]||"")}</label>`).join("")}</div>`; }
  window.setField=function(group,f,v){ state[group]=state[group]||{}; state[group][f]=v; saveState(); refreshStatus(); };
  function chips(list){ return `<div class="placeholder-list">${list.map(x=>`<span class="placeholder">&lt;${esc(x)}&gt;</span>`).join("")}</div>`; }
  function placeholderMap(){ return {...state.project,...state.technical,...state.vgd,...state.rte,...state.calculation,data_document:today(),numar_document:"AUTO-DEMO",revizie:"0",stampila_proiectant:stampText("proiectant"),stampila_vgd:stampText("vgd"),stampila_rte:stampText("rte")}; }
  function renderText(text){ const map=placeholderMap(); return String(text||"").replace(/<([a-zA-Z0-9_ăîâșțĂÎÂȘȚ]+)>/g,(_,k)=>map[k]||`<${k}>`); }
  function missing(text){ const map=placeholderMap(); return [...new Set([...String(text||"").matchAll(/<([a-zA-Z0-9_ăîâșțĂÎÂȘȚ]+)>/g)].map(m=>m[1]).filter(k=>!map[k]))]; }
  function stampText(role){ const s=state.stamps.find(x=>x.role===role); return s ? `[Ștampilă ${role}: ${s.name}]` : `[Ștampilă ${role} lipsă]`; }
  function completion(){ const req=["beneficiar","adresa_lucrare","localitate","judet","osd","proiectant","debit_instalat","presiune_regim","diametru_conducta"]; return Math.round(req.filter(k=>(state.project[k]||state.technical[k]||"").trim()).length/req.length*100); }

  function pageDashboard(){ content(`<div class="grid3"><div class="kpi"><strong>Profil</strong><span>${activeProfile()?"Activ":"Blocat"}</span></div><div class="kpi"><strong>Plan</strong><span>${planLabel()}</span></div><div class="kpi"><strong>Completare</strong><span>${completion()}%</span></div></div><div class="card"><h3>Flux rapid</h3><p>Date proiect → Date tehnice → Documentație → Ștampile → VGD/RTE → Email → Export.</p><div class="actions"><button onclick="openPage('Date proiect')">Date proiect</button><button onclick="openPage('Documentație')">Documentație</button><button onclick="openPage('AI Developer')">AI Developer</button></div></div>${activeProfile()?"":`<div class="card locked">Profil vizibil, dar blocat până la configurare Developer/Service.</div>`}`); }
  function pageProject(){ content(`<div class="card"><h3>Date proiect</h3>${formFor("project",fields.project)}<div class="actions"><button class="primary" onclick="saveProjectSnapshot()">Salvează proiect</button><button onclick="openPage('Documentație')">Trimite către Documentație</button></div></div><div class="card"><h3>Placeholder-e</h3>${chips(fields.project)}</div>`); }
  window.saveProjectSnapshot=function(){ state.projects.unshift({id:Date.now(),name:state.project.beneficiar||"Proiect fără nume",date:today(),status:completion()+"% complet"}); saveState(); toast("Proiect salvat în registru."); };
  function pageTechnical(){ content(`<div class="card"><h3>Date tehnice</h3>${formFor("technical",fields.technical)}<div class="actions"><button class="primary" onclick="saveState();toast('Date tehnice salvate')">Salvează</button><button onclick="openPage('Calcul')">Trimite către Calcul</button></div></div><div class="card"><h3>Placeholder-e tehnice</h3>${chips(fields.technical)}</div>`); }
  function pageDocuments(){ const opts=documentTemplates.map(t=>`<option value="${t.id}">${esc(t.name)}</option>`).join(""); content(`<div class="card"><h3>Motor documente</h3><div class="grid"><label>Tip document<select id="docTpl" onchange="loadDocTpl()">${opts}</select></label><label>Titlu document<input id="docTitle" value="Document tehnic"></label></div><label>Editor document<textarea id="docEditor"></textarea></label><div class="actions"><button onclick="loadDocTpl()">Încarcă șablon</button><button class="primary" onclick="previewDoc()">Previzualizare</button><button onclick="saveDoc()">Salvează document</button><button onclick="guardedExport()">Export DOCX/PDF/ZIP</button></div></div><div class="card"><h3>Previzualizare</h3><pre id="docPreview"></pre><div id="docMissing"></div></div><div class="card"><h3>Documente salvate</h3>${renderDocs()}</div>`); loadDocTpl(); }
  window.loadDocTpl=function(){ const t=documentTemplates.find(x=>x.id===document.getElementById("docTpl").value)||documentTemplates[0]; document.getElementById("docEditor").value=t.body; document.getElementById("docTitle").value=t.name; };
  window.previewDoc=function(){ const raw=document.getElementById("docEditor").value; document.getElementById("docPreview").textContent=renderText(raw); const miss=missing(raw); document.getElementById("docMissing").innerHTML=miss.length?`<p class="status-bad">Placeholder-e lipsă:</p>${chips(miss)}`:`<p class="status-ok">Toate placeholder-ele sunt pregătite.</p>`; };
  window.saveDoc=function(){ previewDoc(); state.documents.unshift({id:Date.now(),title:document.getElementById("docTitle").value,raw:document.getElementById("docEditor").value,rendered:document.getElementById("docPreview").textContent,date:today()}); saveState(); toast("Document salvat."); openPage("Documentație"); };
  function renderDocs(){ return state.documents.length?`<table class="table"><tr><th>Titlu</th><th>Data</th><th>Acțiuni</th></tr>${state.documents.map(d=>`<tr><td>${esc(d.title)}</td><td>${d.date}</td><td><button onclick="downloadDoc(${d.id})">Descarcă TXT</button></td></tr>`).join("")}</table>`:`<p class="muted">Nu există documente salvate.</p>`; }
  window.downloadDoc=function(id){ const d=state.documents.find(x=>x.id===id); if(!d)return; if(!canExport()) return alert("Export blocat pentru planul curent."); const blob=new Blob([d.rendered],{type:"text/plain;charset=utf-8"}); const a=document.createElement("a"); a.href=URL.createObjectURL(blob); a.download=(d.title||"document")+".txt"; a.click(); };
  function pageTemplates(){ content(`<div class="card"><h3>Șabloane OSD</h3><div class="grid"><label>OSD<select><option>Distrigaz Sud Rețele</option><option>Delgaz Grid</option><option>Premier Energy</option><option>Alt OSD</option></select></label><label>Șablon nou<input placeholder="Nume șablon"></label></div><div class="actions"><button>Adaugă șablon</button><button>Scanează placeholder-e</button><button onclick="openPage('Documentație')">Generează document</button></div></div><div class="card locked">Biblioteca OSD completă se configurează prin Developer/Service.</div>`); }
  function pageCalculation(){ content(`<div class="card"><h3>Calcul tehnic</h3>${formFor("calculation",fields.calculation)}<div class="actions"><button class="primary" onclick="runCalc()">Calculează demo</button><button onclick="openPage('Documentație')">Trimite rezultat către Documentație</button></div></div>`); }
  window.runCalc=function(){ const debit=Number(state.technical.debit_instalat||0); const lung=Number(state.technical.lungime_bransament||0); state.calculation.calcul_debit=String(debit); state.calculation.pierdere_presiune=(debit*lung*0.001).toFixed(3); state.calculation.rezultat_calcul=`Calcul demo: debit ${debit}, lungime ${lung}, pierdere estimată ${state.calculation.pierdere_presiune}`; saveState(); openPage("Calcul"); };
  function pageStamps(){ content(`<div class="card"><h3>Ștampile</h3><div class="grid3">${["proiectant","vgd","rte"].map(r=>`<div><h3>${r.toUpperCase()}</h3><input type="file" id="stamp_${r}" accept="image/*"><button onclick="addStamp('${r}')">Încarcă ștampilă ${r}</button></div>`).join("")}</div></div><div class="card"><h3>Ștampile încărcate</h3>${renderStamps()}</div>`); }
  window.addStamp=function(role){ const f=document.getElementById("stamp_"+role).files[0]; state.stamps=state.stamps.filter(x=>x.role!==role); state.stamps.push({role,name:f?f.name:"Ștampilă "+role,date:today()}); saveState(); openPage("Ștampile"); };
  function renderStamps(){ return state.stamps.length?`<table class="table"><tr><th>Rol</th><th>Nume</th><th>Placeholder</th></tr>${state.stamps.map(s=>`<tr><td>${s.role}</td><td>${esc(s.name)}</td><td>&lt;stampila_${s.role}&gt;</td></tr>`).join("")}</table>`:`<p class="muted">Nu există ștampile încărcate.</p>`; }
  function pageEmails(){ const opts=emailTemplates.map(t=>`<option value="${t.id}">${esc(t.name)}</option>`).join(""); content(`<div class="card"><h3>Email-uri</h3><div class="grid"><label>Template<select id="emailTpl" onchange="loadEmailTpl()">${opts}</select></label><label>Destinatar<input id="emailTo" value="${esc(state.project.email)}"></label></div><label>CC<input id="emailCc"></label><label>Subiect<input id="emailSubject"></label><label>Conținut<textarea id="emailBody"></textarea></label><div class="actions"><button onclick="loadEmailTpl()">Încarcă template</button><button class="primary" onclick="prepareEmail()">Pregătește email</button><button onclick="sendMailto()">Deschide email</button></div></div><div class="card"><h3>Email pregătit</h3><pre id="emailPreview"></pre></div>`); loadEmailTpl(); }
  window.loadEmailTpl=function(){ const t=emailTemplates.find(x=>x.id===document.getElementById("emailTpl").value)||emailTemplates[0]; document.getElementById("emailSubject").value=renderText(t.subject); document.getElementById("emailBody").value=renderText(t.body); };
  window.prepareEmail=function(){ const txt=`Către: ${document.getElementById("emailTo").value}\nCC: ${document.getElementById("emailCc").value}\nSubiect: ${document.getElementById("emailSubject").value}\n\n${document.getElementById("emailBody").value}`; document.getElementById("emailPreview").textContent=txt; state.emails.unshift({id:Date.now(),to:document.getElementById("emailTo").value,subject:document.getElementById("emailSubject").value,date:today(),body:document.getElementById("emailBody").value}); saveState(); };
  window.sendMailto=function(){ prepareEmail(); const to=encodeURIComponent(document.getElementById("emailTo").value); const subject=encodeURIComponent(document.getElementById("emailSubject").value); const body=encodeURIComponent(document.getElementById("emailBody").value); window.location.href=`mailto:${to}?subject=${subject}&body=${body}`; };
  function pageVerifications(){ content(`<div class="grid"><div class="card"><h3>Verificare VGD</h3>${formFor("vgd",fields.vgd)}<div class="actions"><button class="primary" onclick="authorize('vgd')">Autorizează VGD</button><button onclick="generateRoleDoc('vgd')">Generează document VGD</button></div></div><div class="card"><h3>Verificare RTE</h3>${formFor("rte",fields.rte)}<div class="actions"><button class="primary" onclick="authorize('rte')">Autorizează RTE</button><button onclick="generateRoleDoc('rte')">Generează document RTE</button></div></div></div><div class="card"><h3>Validări</h3>${renderValidation()}</div>`); }
  window.authorize=function(role){ if(role==="vgd" && (!state.vgd.verificator_vgd || !state.vgd.atestat_vgd)) return alert("Lipsesc date VGD obligatorii."); if(role==="rte" && (!state.rte.responsabil_rte || !state.rte.autorizatie_rte)) return alert("Lipsesc date RTE obligatorii."); state[role]["status_"+role]="admis"; saveState(); openPage("Verificări"); };
  window.generateRoleDoc=function(role){ const tpl=documentTemplates.find(x=>x.id===role); state.documents.unshift({id:Date.now(),title:tpl.name,raw:tpl.body,rendered:renderText(tpl.body),date:today()}); saveState(); toast("Document generat în Documentație."); };
  function renderValidation(){ const missing=[]; ["beneficiar","adresa_lucrare","localitate","judet","osd","proiectant"].forEach(k=>{if(!state.project[k])missing.push(k);}); ["debit_instalat","presiune_regim","diametru_conducta"].forEach(k=>{if(!state.technical[k])missing.push(k);}); return missing.length?`<p class="status-bad">Câmpuri lipsă:</p>${chips(missing)}`:`<p class="status-ok">Nu există erori critice.</p>`; }
  function pageChecklist(){ const items=[["Date proiect",!!state.project.beneficiar&&!!state.project.adresa_lucrare],["Date tehnice",!!state.technical.debit_instalat&&!!state.technical.diametru_conducta],["Documente",state.documents.length>0],["Ștampilă proiectant",state.stamps.some(x=>x.role==="proiectant")],["VGD",state.vgd.status_vgd==="admis"],["RTE",state.rte.status_rte==="admis"],["Email pregătit",state.emails.length>0],["Plan export",canExport()]]; content(`<div class="card"><h3>Checklist proiect</h3><table class="table"><tr><th>Item</th><th>Status</th></tr>${items.map(([n,ok])=>`<tr><td>${n}</td><td class="${ok?'status-ok':'status-bad'}">${ok?'Complet':'Lipsă / blocat'}</td></tr>`).join("")}</table></div>`); }
  function pageRegistry(){ content(`<div class="card"><h3>Registru proiecte</h3>${state.projects.length?`<table class="table"><tr><th>Proiect</th><th>Data</th><th>Status</th></tr>${state.projects.map(p=>`<tr><td>${esc(p.name)}</td><td>${p.date}</td><td>${p.status}</td></tr>`).join("")}</table>`:`<p class="muted">Nu există proiecte salvate.</p>`}</div>`); }
  function pageImportExport(){ content(`<div class="card"><h3>Import / Export</h3><label>Import JSON<textarea id="importText"></textarea></label><div class="actions"><button onclick="importState()">Import</button><button onclick="guardedExport()">Export proiect</button></div><p class="${canExport()?'status-ok':'status-bad'}">Export: ${canExport()?'permis':'blocat pentru planul curent'}</p></div>`); }
  window.importState=function(){ try{ Object.assign(state,JSON.parse(document.getElementById("importText").value)); saveState(); toast("Import aplicat."); }catch{ alert("JSON invalid."); } };
  window.guardedExport=function(){ if(!canExport()) return alert("Export blocat pentru Free/Trial/Basic/Expired. Disponibil pentru Developer/Premium."); const blob=new Blob([JSON.stringify(state,null,2)],{type:"application/json"}); const a=document.createElement("a"); a.href=URL.createObjectURL(blob); a.download="energy_project_services_export.json"; a.click(); };
  function pagePlans(){ content(`<div class="card"><h3>Planuri și licențe</h3>${formFor("plan",["plan","status","activatedAt","expiresAt"])}<div class="actions"><button onclick="setPlan('Free')">Free</button><button onclick="setPlan('Trial')">Trial</button><button onclick="setPlan('Basic')">Basic</button><button class="primary" onclick="setPlan('Developer')">Developer</button></div><p>Export permis: <strong>${canExport()?"DA":"NU"}</strong></p></div><div class="card"><h3>Reguli</h3><pre>${JSON.stringify(state.planRules,null,2)}</pre></div>`); }
  window.setPlan=function(plan){ state.plan.plan=plan; state.plan.status="activ"; if(plan==="Trial") state.plan.expiresAt=addDays(14); if(plan==="Developer") state.plan.expiresAt=addDays(36500); saveState(); openPage("Planuri și licențe"); };
  function addDays(n){ const d=new Date(); d.setDate(d.getDate()+n); return d.toISOString().slice(0,10); }
  function pageMarketplace(){ const mods=["OSD Templates","VGD/RTE","Import OCR","Planuri/Scheme","Marketplace șabloane","Document Engine Pro","Email Engine"]; content(`<div class="grid3">${mods.map(m=>`<div class="card locked"><h3>${m}</h3><p>Modul premium vizibil, blocat până la configurare Developer/Service.</p></div>`).join("")}</div>`); }
  function pageAssistant(){ content(`<div class="card"><h3>Asistent utilizator</h3><div id="userChat" class="chat"><div class="msg ai">Întreabă unde completezi date, ce lipsește pentru documente, export, VGD/RTE sau emailuri.</div></div><div class="grid"><textarea id="userAsk" placeholder="Ex: Ce lipsește pentru generare document?"></textarea><button class="primary" onclick="askAssistant()">Întreabă</button></div></div>`); }
  window.askAssistant=function(){ const q=document.getElementById("userAsk").value; const answer=localAssistant(q); const chat=document.getElementById("userChat"); chat.innerHTML+=`<div class="msg user">${esc(q)}</div><div class="msg ai">${esc(answer)}</div>`; chat.scrollTop=chat.scrollHeight; };
  function localAssistant(q){ q=(q||"").toLowerCase(); if(q.includes("export")) return `Exportul este ${canExport()?"permis":"blocat"} pentru planul ${state.plan.plan}.`; if(q.includes("vgd")) return "VGD se completează la Verificări și folosește ștampila VGD."; if(q.includes("rte")) return "RTE se completează la Verificări și generează document RTE."; if(q.includes("document")) return "Completează Date proiect și Date tehnice, apoi mergi la Documentație."; if(q.includes("email")) return "Email-urile se pregătesc în pagina Email-uri cu template-uri."; return "Paginile principale sunt Date proiect, Date tehnice, Documentație, Ștampile, Verificări VGD/RTE, Email-uri și AI Developer."; }
  function pageAIDeveloper(){ const history=JSON.parse(localStorage.getItem(CHAT_KEY)||"[]"); content(`<div class="card"><h3>AI Developer — chat tip ChatGPT</h3><div id="devChat" class="chat">${history.map(m=>`<div class="msg ${m.role==='user'?'user':'ai'}">${esc(m.text)}</div>`).join("")||`<div class="msg ai">Scrie o comandă. Pot analiza sau genera update prin backend/OpenAI.</div>`}</div><label>Comandă AI Developer<textarea id="devPrompt"></textarea></label><div class="actions"><button class="primary" onclick="sendDevChat()">Trimite comandă</button><button onclick="runDevAnalyze()">Rulează analiză</button><button onclick="runDevUpdate()">Generează și aplică update</button><button onclick="clearDevChat()">Curăță chat</button></div></div><div class="card"><h3>Raport AI Developer</h3><pre id="devReport"></pre></div>`); }
  function addDev(role,text){ const h=JSON.parse(localStorage.getItem(CHAT_KEY)||"[]"); h.push({role,text,date:new Date().toISOString()}); localStorage.setItem(CHAT_KEY,JSON.stringify(h)); pageAIDeveloper(); }
  window.sendDevChat=function(){ const p=document.getElementById("devPrompt").value.trim(); if(!p)return; addDev("user",p); addDev("ai","Comandă primită. Folosește analiză sau update."); };
  window.runDevAnalyze=async function(){ const text=document.getElementById("devPrompt").value; const res=await api("/api/ai-developer/analyze",{method:"POST",body:JSON.stringify({text})}); document.getElementById("devReport").textContent=JSON.stringify(res,null,2); };
  window.runDevUpdate=async function(){ const text=document.getElementById("devPrompt").value; if(!confirm("Rulez update prin GitHub self-update?"))return; const res=await api("/api/update/run",{method:"POST",body:JSON.stringify({text})}); document.getElementById("devReport").textContent=JSON.stringify(res,null,2); addDev("ai",res.ok?"Update generat/aplicat. Verifică GitHub și Render.":"Update eșuat: "+(res.error||"eroare")); };
  window.clearDevChat=function(){ localStorage.removeItem(CHAT_KEY); pageAIDeveloper(); };
  function pageInside(){ content(`<div class="card locked"><h3>Inside Full</h3><p>Acces intern restricționat. Funcțiile distructive sunt dezactivate.</p></div>`); }
  async function pageDiagnostic(){ content(`<div class="card"><h3>Diagnostic</h3><pre id="diag">Se încarcă...</pre><div class="actions"><button onclick="openPage('Diagnostic')">Reverifică</button></div></div>`); const h=await api("/api/health"); document.getElementById("diag").textContent=JSON.stringify({health:h,site:SITE_URL,profile:{industry,workType,active:activeProfile()},plan:state.plan,exportAllowed:canExport(),completion:completion()},null,2); }
  function pageUpdates(){ content(`<div class="card"><h3>Actualizări / Run Update</h3><input id="promptFiles" type="file" multiple><label>Prompt manual<textarea id="manualPrompt"></textarea></label><div class="actions"><button onclick="uploadPrompts()">Upload prompturi</button><button onclick="listPrompts()">Listă prompturi</button><button class="primary" onclick="runUpdate()">Run Update</button></div></div><div class="card"><h3>Log</h3><pre id="updateLog"></pre></div>`); }
  window.uploadPrompts=async function(){ const fd=new FormData(); [...document.getElementById("promptFiles").files].forEach(f=>fd.append("files",f)); const text=document.getElementById("manualPrompt").value; if(text.trim())fd.append("text",text); const r=await fetch("/api/prompts/upload",{method:"POST",body:fd}); const res=await r.json(); document.getElementById("updateLog").textContent=JSON.stringify(res,null,2); };
  window.listPrompts=async function(){ const res=await api("/api/prompts"); document.getElementById("updateLog").textContent=JSON.stringify(res,null,2); };
  window.runUpdate=async function(){ if(!confirm("Rulez Run Update o singură dată?"))return; const text=document.getElementById("manualPrompt").value; const res=await api("/api/update/run",{method:"POST",body:JSON.stringify({text})}); state.updates.unshift({date:today(),ok:res.ok,id:res.id,downloadUrl:res.downloadUrl||""}); saveState(); document.getElementById("updateLog").textContent=JSON.stringify(res,null,2); };
  function pageLaunch(){ content(`<div class="card"><h3>Construire / Lansare</h3><table class="table"><tr><td>Link public</td><td>${location.origin}</td></tr><tr><td>Repository</td><td>dragosserban95 / Energy-Project-Design</td></tr><tr><td>Profil activ</td><td>${industry} / ${workType}</td></tr></table><div class="actions"><button onclick="openPage('Diagnostic')">Diagnostic</button><button onclick="openPage('Actualizări')">Run Update</button></div></div>`); }
  function pageGeneric(p){ content(`<div class="card"><h3>${esc(p)}</h3><p>Pagină pregătită operațional.</p></div>`); }
  boot();
})();
